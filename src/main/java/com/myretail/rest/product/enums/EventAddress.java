package com.myretail.rest.product.enums;

/**
 * Event bus addresseses.
 * <p>
 * Each address represents a different consumer endpoint.
 */
public enum EventAddress {
  GET_PRODUCT,
  GET_PRICE,
  UPDATE_PRICE
}
